n = int(input("Enter number of elements: "))
arr = []

for i in range(n):
    num = int(input(f"Enter element {i+1}: "))
    arr.append(num)
rotated = [arr[-1]] + arr[:-1]

print("Rotated array:", rotated)
